// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2017.2
// Copyright (C) 1986-2017 Xilinx, Inc. All Rights Reserved.
// ==============================================================

#include <systemc>
#include <iostream>
#include <cstdlib>
#include <cstddef>
#include <stdint.h>
#include "SysCFileHandler.h"
#include "ap_int.h"
#include "ap_fixed.h"
#include <complex>
#include <stdbool.h>
#include "autopilot_cbe.h"
#include "ap_stream.h"
#include "hls_stream.h"
#include "hls_half.h"
#include "hls_signal_handler.h"

using namespace std;
using namespace sc_core;
using namespace sc_dt;


// [dump_struct_tree [build_nameSpaceTree] dumpedStructList] ---------->


// [dump_enumeration [get_enumeration_list]] ---------->


// wrapc file define: "state"
#define AUTOTB_TVIN_state  "../tv/cdatafile/c.encrypt_128_key_expand_inline_no_branch.autotvin_state.dat"
#define AUTOTB_TVOUT_state  "../tv/cdatafile/c.encrypt_128_key_expand_inline_no_branch.autotvout_state.dat"
// wrapc file define: "key"
#define AUTOTB_TVIN_key  "../tv/cdatafile/c.encrypt_128_key_expand_inline_no_branch.autotvin_key.dat"

#define INTER_TCL  "../tv/cdatafile/ref.tcl"

// tvout file define: "state"
#define AUTOTB_TVOUT_PC_state  "../tv/rtldatafile/rtl.encrypt_128_key_expand_inline_no_branch.autotvout_state.dat"

class INTER_TCL_FILE {
	public:
		INTER_TCL_FILE(const char* name) {
			mName = name;
			state_depth = 0;
			key_depth = 0;
			trans_num =0;
		}

		~INTER_TCL_FILE() {
			mFile.open(mName);
			if (!mFile.good()) {
				cout << "Failed to open file ref.tcl" << endl;
				exit (1);
			}
			string total_list = get_depth_list();
			mFile << "set depth_list {\n";
			mFile << total_list;
			mFile << "}\n";
			mFile << "set trans_num "<<trans_num<<endl;
			mFile.close();
		}

		string get_depth_list () {
			stringstream total_list;
			total_list << "{state " << state_depth << "}\n";
			total_list << "{key " << key_depth << "}\n";
			return total_list.str();
		}

		void set_num (int num , int* class_num) {
			(*class_num) = (*class_num) > num ? (*class_num) : num;
		}
	public:
		int state_depth;
		int key_depth;
		int trans_num;

	private:
		ofstream mFile;
		const char* mName;
};

extern "C" void encrypt_128_key_expand_inline_no_branch (
int state[4],
int key[4]);

extern "C" void AESL_WRAP_encrypt_128_key_expand_inline_no_branch (
int state[4],
int key[4])
{
	refine_signal_handler();
	fstream wrapc_switch_file_token;
	wrapc_switch_file_token.open(".hls_cosim_wrapc_switch.log");
	int AESL_i;
	if (wrapc_switch_file_token.good())
	{
		CodeState = ENTER_WRAPC_PC;
		static unsigned AESL_transaction_pc = 0;
		string AESL_token;
		string AESL_num;
		static AESL_FILE_HANDLER aesl_fh;


		// output port post check: "state"
		aesl_fh.read(AUTOTB_TVOUT_PC_state, AESL_token); // [[transaction]]
		if (AESL_token != "[[transaction]]")
		{
			exit(1);
		}
		aesl_fh.read(AUTOTB_TVOUT_PC_state, AESL_num); // transaction number

		if (atoi(AESL_num.c_str()) == AESL_transaction_pc)
		{
			aesl_fh.read(AUTOTB_TVOUT_PC_state, AESL_token); // data

			sc_bv<32> *state_pc_buffer = new sc_bv<32>[4];
			int i = 0;

			while (AESL_token != "[[/transaction]]")
			{
				bool no_x = false;
				bool err = false;

				// search and replace 'X' with "0" from the 1st char of token
				while (!no_x)
				{
					size_t x_found = AESL_token.find('X');
					if (x_found != string::npos)
					{
						if (!err)
						{
							cerr << "WARNING: [SIM 212-201] RTL produces unknown value 'X' on port 'state', possible cause: There are uninitialized variables in the C design." << endl;
							err = true;
						}
						AESL_token.replace(x_found, 1, "0");
					}
					else
					{
						no_x = true;
					}
				}

				no_x = false;

				// search and replace 'x' with "0" from the 3rd char of token
				while (!no_x)
				{
					size_t x_found = AESL_token.find('x', 2);

					if (x_found != string::npos)
					{
						if (!err)
						{
							cerr << "WARNING: [SIM 212-201] RTL produces unknown value 'X' on port 'state', possible cause: There are uninitialized variables in the C design." << endl;
							err = true;
						}
						AESL_token.replace(x_found, 1, "0");
					}
					else
					{
						no_x = true;
					}
				}

				// push token into output port buffer
				if (AESL_token != "")
				{
					state_pc_buffer[i] = AESL_token.c_str();
					i++;
				}

				aesl_fh.read(AUTOTB_TVOUT_PC_state, AESL_token); // data or [[/transaction]]

				if (AESL_token == "[[[/runtime]]]" || aesl_fh.eof(AUTOTB_TVOUT_PC_state))
				{
					exit(1);
				}
			}

			// ***********************************
			if (i > 0)
			{
				// RTL Name: state
				{
					// bitslice(31, 0)
					// {
						// celement: state(31, 0)
						// {
							sc_lv<32>* state_lv0_0_3_1 = new sc_lv<32>[4];
						// }
					// }

					// bitslice(31, 0)
					{
						int hls_map_index = 0;
						// celement: state(31, 0)
						{
							// carray: (0) => (3) @ (1)
							for (int i_0 = 0; i_0 <= 3; i_0 += 1)
							{
								if (&(state[0]) != NULL) // check the null address if the c port is array or others
								{
									state_lv0_0_3_1[hls_map_index].range(31, 0) = sc_bv<32>(state_pc_buffer[hls_map_index].range(31, 0));
									hls_map_index++;
								}
							}
						}
					}

					// bitslice(31, 0)
					{
						int hls_map_index = 0;
						// celement: state(31, 0)
						{
							// carray: (0) => (3) @ (1)
							for (int i_0 = 0; i_0 <= 3; i_0 += 1)
							{
								// sub                    : i_0
								// ori_name               : state[i_0]
								// sub_1st_elem           : 0
								// ori_name_1st_elem      : state[0]
								// output_left_conversion : state[i_0]
								// output_type_conversion : (state_lv0_0_3_1[hls_map_index]).to_uint64()
								if (&(state[0]) != NULL) // check the null address if the c port is array or others
								{
									state[i_0] = (state_lv0_0_3_1[hls_map_index]).to_uint64();
									hls_map_index++;
								}
							}
						}
					}
				}
			}

			// release memory allocation
			delete [] state_pc_buffer;
		}

		AESL_transaction_pc++;
	}
	else
	{
		CodeState = ENTER_WRAPC;
		static unsigned AESL_transaction;

		static AESL_FILE_HANDLER aesl_fh;

		// "state"
		char* tvin_state = new char[50];
		aesl_fh.touch(AUTOTB_TVIN_state);
		char* tvout_state = new char[50];
		aesl_fh.touch(AUTOTB_TVOUT_state);

		// "key"
		char* tvin_key = new char[50];
		aesl_fh.touch(AUTOTB_TVIN_key);

		CodeState = DUMP_INPUTS;
		static INTER_TCL_FILE tcl_file(INTER_TCL);
		int leading_zero;

		// [[transaction]]
		sprintf(tvin_state, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVIN_state, tvin_state);

		sc_bv<32>* state_tvin_wrapc_buffer = new sc_bv<32>[4];

		// RTL Name: state
		{
			// bitslice(31, 0)
			{
				int hls_map_index = 0;
				// celement: state(31, 0)
				{
					// carray: (0) => (3) @ (1)
					for (int i_0 = 0; i_0 <= 3; i_0 += 1)
					{
						// sub                   : i_0
						// ori_name              : state[i_0]
						// sub_1st_elem          : 0
						// ori_name_1st_elem     : state[0]
						// regulate_c_name       : state
						// input_type_conversion : state[i_0]
						if (&(state[0]) != NULL) // check the null address if the c port is array or others
						{
							sc_lv<32> state_tmp_mem;
							state_tmp_mem = state[i_0];
							state_tvin_wrapc_buffer[hls_map_index].range(31, 0) = state_tmp_mem.range(31, 0);
                                 	       hls_map_index++;
						}
					}
				}
			}
		}

		// dump tv to file
		for (int i = 0; i < 4; i++)
		{
			sprintf(tvin_state, "%s\n", (state_tvin_wrapc_buffer[i]).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVIN_state, tvin_state);
		}

		tcl_file.set_num(4, &tcl_file.state_depth);
		sprintf(tvin_state, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVIN_state, tvin_state);

		// release memory allocation
		delete [] state_tvin_wrapc_buffer;

		// [[transaction]]
		sprintf(tvin_key, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVIN_key, tvin_key);

		sc_bv<32>* key_tvin_wrapc_buffer = new sc_bv<32>[4];

		// RTL Name: key
		{
			// bitslice(31, 0)
			{
				int hls_map_index = 0;
				// celement: key(31, 0)
				{
					// carray: (0) => (3) @ (1)
					for (int i_0 = 0; i_0 <= 3; i_0 += 1)
					{
						// sub                   : i_0
						// ori_name              : key[i_0]
						// sub_1st_elem          : 0
						// ori_name_1st_elem     : key[0]
						// regulate_c_name       : key
						// input_type_conversion : key[i_0]
						if (&(key[0]) != NULL) // check the null address if the c port is array or others
						{
							sc_lv<32> key_tmp_mem;
							key_tmp_mem = key[i_0];
							key_tvin_wrapc_buffer[hls_map_index].range(31, 0) = key_tmp_mem.range(31, 0);
                                 	       hls_map_index++;
						}
					}
				}
			}
		}

		// dump tv to file
		for (int i = 0; i < 4; i++)
		{
			sprintf(tvin_key, "%s\n", (key_tvin_wrapc_buffer[i]).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVIN_key, tvin_key);
		}

		tcl_file.set_num(4, &tcl_file.key_depth);
		sprintf(tvin_key, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVIN_key, tvin_key);

		// release memory allocation
		delete [] key_tvin_wrapc_buffer;

// [call_c_dut] ---------->

		CodeState = CALL_C_DUT;
		encrypt_128_key_expand_inline_no_branch(state, key);

		CodeState = DUMP_OUTPUTS;

		// [[transaction]]
		sprintf(tvout_state, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVOUT_state, tvout_state);

		sc_bv<32>* state_tvout_wrapc_buffer = new sc_bv<32>[4];

		// RTL Name: state
		{
			// bitslice(31, 0)
			{
				int hls_map_index = 0;
				// celement: state(31, 0)
				{
					// carray: (0) => (3) @ (1)
					for (int i_0 = 0; i_0 <= 3; i_0 += 1)
					{
						// sub                   : i_0
						// ori_name              : state[i_0]
						// sub_1st_elem          : 0
						// ori_name_1st_elem     : state[0]
						// regulate_c_name       : state
						// input_type_conversion : state[i_0]
						if (&(state[0]) != NULL) // check the null address if the c port is array or others
						{
							sc_lv<32> state_tmp_mem;
							state_tmp_mem = state[i_0];
							state_tvout_wrapc_buffer[hls_map_index].range(31, 0) = state_tmp_mem.range(31, 0);
                                 	       hls_map_index++;
						}
					}
				}
			}
		}

		// dump tv to file
		for (int i = 0; i < 4; i++)
		{
			sprintf(tvout_state, "%s\n", (state_tvout_wrapc_buffer[i]).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVOUT_state, tvout_state);
		}

		tcl_file.set_num(4, &tcl_file.state_depth);
		sprintf(tvout_state, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVOUT_state, tvout_state);

		// release memory allocation
		delete [] state_tvout_wrapc_buffer;

		CodeState = DELETE_CHAR_BUFFERS;
		// release memory allocation: "state"
		delete [] tvin_state;
		delete [] tvout_state;
		// release memory allocation: "key"
		delete [] tvin_key;

		AESL_transaction++;

		tcl_file.set_num(AESL_transaction , &tcl_file.trans_num);
	}
}

